<% if breadcrumbs? %>
<ul id='breadcrumbs'>
  <% breadcrumbs do |name, url, current| %>
    <li class="<%= current ? 'selected' : '' %>">
      <%= link_to name, url %>
      <% unless current %>
        <span>::</span>
      <% end %>
    </li>
  <% end %>
</ul>
<% end %>
